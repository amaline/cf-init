machine:
  ruby:
    version: 1.9.3-p448
    
dependencies:
  override:
    - gem install bundler
    - cd deployments/cf-example; bundle install
    # - cd deployments/cf-example; bosh aws create
    - find / -name vpc.rb -print
    # - sed -i -e 's/default_dhcp_opts\.delete/#default_dhcp_opts.delete/' /usr/local/bundle/gems/bosh_cli_plugin_aws-*/lib/bosh_cli_plugin_aws/vpc.rb
    # - cat /usr/local/bundle/gems/bosh_cli_plugin_aws-*/lib/bosh_cli_plugin_aws/vpc.rb
    
general:
  branches:
    only:
      - master # list of branches to build
      - /feature-.*/ # or regexes
  artifacts:
    - "deployments/cf-example" # relative to the build directory
